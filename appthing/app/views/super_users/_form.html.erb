<%= form_for(@super_user) do |f| %>
  <% if @super_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@super_user.errors.count, "error") %> prohibited this super_user from being saved:</h2>

      <ul>
      <% @super_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
